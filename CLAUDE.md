@AGENTS.md

# Claude Code Notes

`AGENTS.md` is the canonical repository guide. Read the relevant current source
and tests before loading dated architecture or parity documents.

- Use `CLAUDE.local.md` for machine-only preferences.
- Do not create subagents, commit, push, deploy, publish, or mutate live
  services unless the user requests that outcome.
